mutation registration($name: String!, $password: String!) {
    registration(name: $name, password: $password){
        user {
            _id
            name
        }
        token
        error
    }
}